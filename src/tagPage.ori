(posts, tag) => page.ori({
  title: tag
  @text: `
    <h1>${ tag }</h1>
    <ol reversed>

    ${ map(posts, (post,filename) => `
      <li>
        <a href="/posts/${ filename }">${ post/title }</a>
      </li>
    `) }

    </ol>
  `
})