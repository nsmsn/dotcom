(items, tag) => page.ori({
  title: tag
  @text: `
    <h1>Tagged '${ tag }'</h1>
    
    <ol reversed>

    ${ map(items, (item,filename) => `
      <li>
        <a href="${ item/path }">${ item/title }</a>
      </li>
    `) }

    </ol>
  `
})