(notes) => page.ori({
  title: "Notes"
  @text: `
    <h1>${ title }</h1>
    <ol reversed>

    ${ map(notes, (note) => `
      <li>
        <a href="${ note/path }">${ date.ori(note/date) }</a>
      </li>
    `) }

    </ol>
  `
})